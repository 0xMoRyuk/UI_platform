# Utils

Pure utility functions with no side effects.

## Guidelines

- Keep functions pure and testable
- Use TypeScript
- Document with JSDoc for complex utilities
- No React-specific code (use hooks/ for that)

## Examples

- `formatBytes()` - Format byte sizes for display
- `debounce()` - Debounce function calls
- `compressImage()` - Image compression utilities
